apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: informixtest-pvc
  namespace: informixtest
spec:
  storageClassName: ""
  volumeName: informixtest-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
